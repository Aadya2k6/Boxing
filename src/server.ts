// Server entry point — Nitro-compatible (for Vercel deployment)
// Nitro handles the server lifecycle; we just re-export the TanStack Start entry.
export { default } from "@tanstack/react-start/server-entry";
